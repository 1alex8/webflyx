Inititalize Repo
    Command : git init
            Inititalizes a repo, must be executed in the working dir

Show hidden content of dir
    Command: ls -hidden / ls -a
            Shows all Dirs even the hidden ones 1 for Ps and 2 for bash

A File can be one of several states in a git Repo. Important ones:

            - untracked: Not being tracked by git
            - staged: Marked for inclusion in the next commit
            - committed : Saved to the repo's history
        
Status
    Command: git status 
            Shows you the current state of your repo. It will tell
            you which files are untracked, staged and committed

Add to Repo
    git add [path to file] || git add .
            first adds a single file to what will be committed; second adds everything
            use "git rm --cached <file>..." to unstage

Commit:
    git commit -m "your message here"

log
    command: git log
            shows a history of commits in a repo.
            - Who made the commit
            - When the commit was made
            - What was changed
    
    git --no-pager log -n 10 --oneline --parents --graph
            no-pager: does it through stdout//doesn't exist on PowerShell
            -n 10 : last 10 commits
            --oneline: google it
            --parents : shows parents
            --graph is a graphical illustration of how branches merged and more

        Most used by me:
        git log -n 10 -- parents --graph
        git --no-pager log -n 10 --parents --graph
        
            to not end up in the lesser or pager view 
            if i should not use no-pager:
                q: to get out of the pager view
                Space : to scroll a page down
                b: to scroll a page up
                /Searchterm: search
        
        git log --oneline
            The  commit hashes are only the necessary chars
        git log --oneline --graph --all
        git log --oneline --decorate --graph --all --parents


Hash
    The hash is the identifier for commits, it is cryptograhic via SHA-1
    It is moded by.
    The authors name and mail
    The time and location
    and other shit 
    the first 7 chars should always be able to identify the author in the same repo

    command: git cat-file -p <hash>
            allos us to see the contents of a commit
            my Hash example: 5a3b9cc371443f76de49f4be40b278680b925219.
            after adding my personal which would be the first 7 i see the hash of the tree,
            if i now use git catfile <tree-hash> it displays the contets of the tree.
            if i now use the <file hash> it displays the content of the file
            <File-hash> is after "blob"

Trees and Blobs
    tree: Git's way of storing a directory
    blob: Gis's way of storing a file

Storing Data:
    Git stores entire Snapshots of files on a per-commit level. Not as thought: only the changes

Optimization: 
    It does have some performance Optimizations:
    - git compresses and packs files to store them more efficiently
    - git deduplicates files that are the same across diffrent commits.
      If a File doesn't change between commits, Git will only store it once



Config:
    git config --add --global user.name "Github/Codeberg username"
    git config --add --global user.email "Github/Codeberg E-mail"

        Take apart:
            git config: the command to interact with the .gitconfig File
            --add :      Flag stating you want to add a configuration
            --global:    Flag stating you want this cofig to be stored globally in your .gitconfig File The opposite is "local", which stores the configuration in the current repo only
            user:        The section
            name:        The key within the section
            "1alex8"     The value you want to set for the key
        
        we can also store anything in it, but you should only do it in the "local" .gitconfig
            Example:
            git config --add --local company.ceo "Alex"
            git config --add --local company.cto "Felix"
    
    git config --get <key>
            useful to get a single value: git config --get user.email
    
    git config --unset <key>
        it needs a specific section and key, then deletes that shit.
    git config --unset-all <key>
        it unsets all instances of that section.key for example if you have multiple instances of : company.ceo
        best example: if you have changed the init.defaultbranch a few times and want to clean up

    git config --remove-section <section>
        used to remove entire section out of the .gitconfig (don't forget to use local if you want to do that on your local gitconfig)

Locations:
    There are several locations where Git can be configured . From more general to more specific:
    - system: /etc/gitconfig; a file that configures git for all users on the system
    - global: ~/.gitconfig; a file that configures git for all projects of a user
    - local: .git/config; a file that configures git for a specific projects
    - worktree: ./git/config.worktree; a file that configures git for a part of a project

    Overrides:
        If you set a config in a more specific location, it will override the same configuration in a more general location.
        For Example: user.name in local overrides user.name in global; user.name in global overrides user.name in system



Branch:
        A Git branch allows you to keep track of diffrent changes seperately.

        For example, let's say you have a big web project and you want to experiment with changing the color scheme. 
        Instead of changing the entire project directly (as of right now, our master branch), 
        you can create a new branch called color_scheme and work on that branch. When you're done, if you like the changes, 
        you can merge the color_scheme branch back into the master branch to keep the changes. 
        If you don't like the changes, you can simply delete the color_scheme branch and go back to the master branch.

    Check which branch you're currently on:
        command: git branch

    Rename: branch
        command: git branch -m oldname newname


        Github's default master branch changed the name to "main". If you work primarily with github or Codeberg
        you should name your init.defaultBranch "main"

        Rename global init.defaultBranch:
            git config --global --unset-all init.defaultBranch
            git config --list
            git config --add --global init.defaultBranch main


    New Branch:
        Create a new Branch, but stay in main branch:
            command: git branch my_new_branch
        Create a new Branch and switch to it:
            command: git switch -c my_new_branch

            The -c flag tells Git to create a new branch if it doesn't already exist.

            if we want to make a branch of an older commit :
            git switch -c new_branch_name <old commit hash>
        
        When you create a new branch, it uses the current commit you are on as the branch base. 
        For Example: 
        if you are on main branch with 3 commits, A, B, C and then run :
            git switch -c my_new_branch
        it will look like this:
            add_classics
        A B C 
             main
        
        You can view the logs of a branch:
            command git log <branch_name>


Merge:
        "What's the point of having multiple branches?" you might ask. 
        They're most often used to safely make changes without affecting your (or your team's) primary branch. 
        However, once you're happy with your changes, 
        you'll want to merge them back into the main branch so that they make their way into the final product.

    We merge add_classics into main by runnig this while on main:
        command: git merge add_classics

        This merge will:
        - find the merge base commit, or best common ancestor of the two branches
        - Replays the changes from add_classics onto main starting from the Merge_base
        - Records the result as a new commit
        - The new commit is special because it has two parents

        If we do this: we end up in vim:
        - to write press : :i
        - enter your merge message
        - press: ESC
        - press: :wq
        - press: Enter

    Common Workflow when merging:
        1. Create a Branch
        2. Make the change
        3. Merge the branch into main
        4. Remove the branch
        5. Repeat

            I have created a Branch 
            made a change in add_new_classics
            made a change in add_new_classics
            merged
            deleted add_new_classics
        
        delete branch, but keep in mind to be in main
        command: git branch -d add_new_clasics

Rebase:

    Visualize Rebase:
        Say we have this commit history:
        A - B - C    main
        \
         D - E      feature_branch

        We're working on feature_branch, and want to bring in the changes our team added to main 
        so we're not working with a stale branch. 
        We could merge main into feature_branch, but that would create an additional merge commit. 
        Rebase avoids a merge commit by replaying the commits from feature_branch on top of main. 
        After a rebase, the history will look like this:

        A - B - C    main
                \
                D - E  feature_branch

    Rebase command:
        
        So if we create a branch from an old hash:
        git switch -c new_branch_name <old commit hash>

        and have made changes we can now put feature branch at the top by:

        git rebase main

        Important: we need to be in feature_branch and rebase main not the other way around.

        IF IT ALL FAILS AND WE HAVE A DOZEN REBASE CONFLICTS, WE CAN REVERT THE REBASE,
        TO THE STATE BEFORE THE REBASE:

            command: git rebase --abort

        
    When to rebase
        git rebase and git merge are different tools.

        An advantage of merge is that it preserves the true history of the project. 
        It shows when branches were merged and where. 
        One disadvantage is that it can create a lot of merge commits, 
        which can make the history harder to read and understand.

        A linear history is generally easier to read, understand, and work with. 
        Some teams enforce the usage of one or the other on their main branch, 
        but generally speaking, you'll be able to do whatever you want with your own branches.

    WARNING
        you should never rebase a public branch (like main) onto anything else. Other devs have it checked out.
        And if you change its history, you'll cause a lot of problems for them

        However, if your own branch, you can rebase onto other branches (including main) as much as you want.


RESET

    Undoing changes
            One of the majo benefits of using Git is the ability to undo changes. There are a lot of diffrent ways to do this, 
            but first, we'll start by going back in the commit history without discarding changes

        GIT RESET SOFT
            The git reset command can be used to undo the last commit(s) 
            or any changes in the index (staged but not committed changes) 
            and the worktree (unstaged and not committed changes).

            Command: git reset --soft COMMITHASH

            The --soft option is useful if you just want to go back to a previous commit, 
            but keep all of your changes. Committed changes will be uncommitted and staged, 
            while uncommitted changes will remain staged or unstaged as before.

        Diffrence between Committed and staged:

            Command: git diff --staged

        GIT RESET HARD 
            In the last lesson, we undid a commit but kept the changes. 
            We don't want to keep the changes to titles.md, 
            here's how to reset those changes:

            Command: git reset --hard COMMITHASH

            This is useful if you just want to go back to a previous commit and discard all the changes.

        DANGER
            I want to stress how dangerous this command can be. If you were to simply delete a committed file, 
            it would be trivially easy to recover because it is tracked in Git. 
            However, if you used git reset --hard to undo committing that file, it would be deleted for good.

            Always be careful when using git reset --hard. It's a powerful tool, but it's also a dangerous one.

        RESET TO A SPECIFIC COMMIT 
            If you want to reset back to a specific commit, 
            you can use the git reset --hard command and provide a commit hash. For example:

            Command: git reset --hard <COMMITHASH>

            This will reset your working directory and index to the state of that commit, 
            and all the changes made after that commit are lost forever.

            Again, be super careful with this. In part 2 of this course, 
            we'll cover more advanced (read: safer) ways to undo changes.


GIT REMOTE
            Often our frenemies (read: coworkers) make code changes that we need to 
            begrudgingly accept into our pristine bug-free repos. /s

            This is where the "distributed" in "distributed version control system" comes from. 
            We can have "remotes", which are just external repos with mostly the same Git history as our local repo.

            When it comes to Git (the CLI tool), there really isn't a "central" repo. 
            GitHub is just someone else's repo. Only by convention and convenience have we, 
            as developers, started to use GitHub as a "source of truth" for our code.

    Adding a remote
            In git, another repo is called a "remote". The standard convention is that when you're treating
            the remote as the "authoritative source of truth" (such as github) you would name it "origin".

            By "authoritative source of truth" we mean that it's the one you and yor team
            treat as the "true" repo. It's the one that contains the most up-to-date version
            of the accepted code

            command: git remote add <name> <uri>